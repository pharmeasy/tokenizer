env: development
project_name: transformer
prefix: dev
namespace: "development"
fqdn: "{{ prefix }}-{{ project_name }}.dev.pharmeasy.in"
tag: "{{ prefix }}-{{ build_number }}"
context: development
env_during_build: True
min_replicas: 1
max_replicas: 1
profile: default
